define(['src/views/AppView'],function (AppView) {
    var ScheduleView = AppView.extend({
        title: "课程表",
//        template: template
       // data: data2
    });

    return ScheduleView;
});
